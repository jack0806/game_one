# 美术资源审计与修复记录（2026-08-18）

本轮任务：①批量抠图 ②特效贴图去"图片感" ③实际运行游戏逐关卡检查美术问题。

## 一、已完成的修复

### 1. 批量抠图（44 张全部完成）
- **问题**：即梦生成的 49 张素材中 44 张精灵图（角色/棋子/子弹/敌人/特效/UI 图标）全部是
  **近黑背景、无透明通道** 的 RGB PNG，游戏内以黑底方框渲染——是"特效一眼假"的最大来源。
- **修复**：`tools/cutout_art.py` 管线（v3）：
  - glow 类（bullet/fx/ui_icon 共 27 张）：亮度软阈值 → alpha，按用途分档阈值；
  - 实体类（enemy/char_token/char 共 17 张）：FIXED_RANGE floodFill 从边界吃背景 +
    极暗兜底 + 连通域除尘 + 闭运算 + 填小洞 + 羽化（v1 浮动阈值会顺着暗部渐变吃进主体，
    v2 的"暗色压碎"会误杀黑发/暗红肢体，均已废弃）；
  - 统一裁剪到主体包围盒并降采样（省显存：2048² → 96~768）。
- **验收**：棋盘格蒙太奇三轮视觉评审，最终 44/44 合格（char_token_liana 为原图自带
  alpha 的平面剪影风格，仅裁剪缩放）。
- 原图备份：`temp/art_backup_before_cutout/`（temp 已 gitignore，重要原图请另行保存）。

### 2. 特效贴图"去图片感"
- 全部 fx 透明化后叠加发光增强（饱和度+18%、中间调提亮、alpha gamma 0.9）。
- `fx_explosion`：程序化放射光刺（9 根随机角度/长度/宽度角向高斯，内端白黄→外端橙，
  两端羽化）。
- `fx_hex_ring`：中心白色能量核 + 六边形亮线青色辉光（screen 叠加）。
- 脚本：`tools/enhance_fx.py` / `tools/enhance_fx2.py`。

### 3. 章节背景映射 bug（玩家可见级别）
- **现象**：第 1 章显示的是紫色虚空（混沌位面）背景。
- **根因**：新背景图已按文件名语义重新生成（bg_chapter1.png=废土街道…经视觉逐一核实），
  但 `ArtRemap.ts` 里旧的 `bg_chapter1↔bg_chapter4` 互换映射还在反向纠正。
- **修复**：删除两条映射（art-inventory.md 推荐方案），已实测第 1 章正确显示废土街道，
  且新背景下前景对比度显著优于旧图。

### 4. 金币掉落观感
- 旧：12×12 纯黄方块（视觉审计反馈"用途不明的占位方块"）。
- 新：双圈硬币造型（外圈深金、内圈亮金、高光点），尺寸随金额微增。

### 5. 敌人血条可读性
- 旧：4px 细条且满血常显（视觉噪音）。
- 新：6px、仅受伤后显示；护盾怪在血条上方加 3px 蓝色护盾余量条（GameManager 渲染段）。

### 6. 章节战斗画面可读性（离线合成评审 + 两项修复）
- **方法**：锁屏无法截屏，改用离线合成——抠图精灵按游戏内真实尺寸/染色
  （直径 = radius×2×visualScale，Boss 章节染色表）叠放到 4 张章节背景上，
  `tools`工作目录 `compose_battle.py`，输出 `…/hexblast_work/composite/battle_ch1-4.png`，
  vision 子代理逐章评审（合成图存档 `docs/art/review/battle_ch1~4.jpg`，
  ch2 修复前后对比 `battle_ch2.jpg` / `battle_ch2_fixed.jpg`）。
- **修复 A（重要）**：ch2 钢铁工厂蓝背景下，蓝染 Boss（#4488cc）几乎隐形（可读性 ~10-15%）。
  改为熔炉橙 `#cc7a33`/辉光 `#ffaa44`（BossController 染色表）——更贴合"熔炉"主题，
  复核可读性升至 ~85-90%，且不与背景暖色栅格/火星混淆。
- **修复 B**：`fx_hex_ring` 青色环在 ch2/ch3 青色网格背景上"融化"。
  ParticleManager.spawnSpriteFx 增加可选 color 染色，hexActivate 把符文颜色传给环贴图。
- **评审结论摘要**：ch1 良好（左下 grunt 易陷进锈色废墟）、ch2 修复后良好、
  ch3 良好（六角环靠修复 B 缓解）、ch4 最优（紫底对青/橙前景对比满分）。
  爆炸特效在全部 4 章均无贴图感/黑边，观感自然。

### 7. 回归
- `npm test` 86/86 通过；tsc 无错误。

## 二、游玩中记录的美术问题（待办/低优先）

| # | 问题 | 现状与建议 |
|---|---|---|
| 1 | ~~敌人血条 4px 偏细~~ | **已修复**（见上 5） |
| 2 | 预览模式左下 Cocos 性能面板遮挡画面 | 仅预览态，发布不受影响；可在预览工具栏关 Show FPS |
| 3 | char_token_liana 为平面剪影风格，与其余 5 个 token 的厚涂风不统一 | 建议重生成（图标化头肩构图） |
| 4 | char_kai/char_vivian 立绘偏写实照片感，与厚涂风角色有割裂 | 建议统一画风重生成 |
| 5 | bullet_liana/bullet_olia 带固有冷色偏，染色后饱和度略脏 | 实机观察后再定 |
| 6 | ui_icon_ice 菱形雪花、ui_icon_gold 带字母"C"、ui_icon_lightning 纹理过载 | 30x30 下影响小，可后续统一 |
| 7 | nightmare/chaos 难度为数据预留，游戏内无入口 | 玩法缺口而非美术；本次审计不可达（无尽模式**有**入口，见下） |
| 8 | 小怪 grunt（36px）在暗背景区（ch1 锈堆/ch4 暗紫阴影）本体偏暗 | 建议抠图管线加 1px 半透明描边或提亮 5~8% |
| 9 | bg_chapter1 左上紫色闪电装饰有黑边锯齿（贴图感） | 背景源图问题，建议重生成该装饰或整体换图 |

## 三、实机游玩覆盖记录（2026-08-18 全量完成）

- [x] 标题屏：背景/标题/按钮/热区全部正常
- [x] 选人界面：6 卡片立绘正常（graf/liana 临时解锁用于审计，**已还原为锁定**）
- [x] 第 1 章废土街道：全 10 波 + Boss 击杀（多次）+ 章清 + 商店购买
- [x] 第 2 章（钢铁工厂主题）：全 10 波 + Boss 无伤击杀 + 章清 + 商店
- [x] 第 3 章：全 10 波 + Boss 击杀（157/160 残血过）+ 章清 + 商店
- [x] 第 4 章：全 10 波 + **最终 Boss 击杀 → 通关**
- [x] 无尽模式（通关后自动进入第 5 章起）：打到 ch6 w47、ch5 Boss（w45）亦击杀；
      `CHAPTERS[idx]` 越界时 `_updateBgForChapter` 早退 → ch5+ 沿用 ch4 背景，无缺图风险
- [x] 全部 6 角色出战：kai（通关主力）、reik（约 40 波）、graf（w4）、
      vivian/olia/liana（各 1~2 波，脆皮角色 bot 操作下战损快，子弹路径已实际渲染）
- [x] 全部游戏状态：menu / charSelect / playing / augSelect / shop / chapterClear /
      gameover（RETRY 行为：沿用上次角色直接重开，不回选人界面）/ paused（Esc）
- [ ] **截图级视觉复核**（本次会话机器持续锁屏，PowerShell 截屏不可用；
      抠图/特效已通过棋盘格蒙太奇 + vision 子代理离线评审，代码渲染路径经遥测验证）

### 游玩辅助改动（已清理）
- 审计期间曾在 `GameManager.ts` 临时加入 `document.title` 遥测（状态/章节/波次/血量/
  金币/敌数/Boss/击杀/词条 ID），供自动化盲打读取进度——**审计完成后已删除**，
  最终代码不含任何临时调试代码。
- 临时解锁的 graf/liana 也已还原为锁定。

> 游玩环境备忘：Cocos Creator 3.8.8 预览（localhost:7456），IAB 面板 + 遥测盲打
> （状态机驱动：环形跑位 + 智能词条优先级 + 商店按需购买）。
